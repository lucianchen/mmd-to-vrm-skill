{
  "name": "mmd-to-vrm-skill",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {"node": ">=22"},
  "scripts": {"test": "node --test tests/*.test.mjs", "compare": "node scripts/serve_compare.mjs", "build:compare": "node scripts/build_compare.mjs"},
  "dependencies": {
    "@pixiv/three-vrm": "3.4.4",
    "@playwright/test": "1.57.0",
    "esbuild": "0.25.12",
    "three": "0.169.0"
  }
}
